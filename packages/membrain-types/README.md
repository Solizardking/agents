# @openclawdsolana/membrain-types

TypeScript types + gRPC client for the Membrain selective-memory layer.

This package is the catalog alias. The implementation lives next door:

- Types + client: [`../membrain/clients/typescript`](../membrain/clients/typescript)
- OpenClawd plugin: [`../membrain/clients/openclawd`](../membrain/clients/openclawd)
- Node host used by `ct-agents memory`: [`../../robinhood-src/membrainMemory.js`](../../robinhood-src/membrainMemory.js)

```bash
ct-agents memory status
```
